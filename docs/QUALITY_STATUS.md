# Quality migration status

The v4 language migration is complete only after the branch passes Japanese Site QA, is merged to `main`, the hourly sync re-renders current Daily/Live content, and the Pages deployment passes all fail-closed quality gates.
